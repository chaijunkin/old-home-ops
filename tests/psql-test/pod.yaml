apiVersion: v1
kind: Pod
metadata:
  name: psql-test-pod
  namespace: database
spec:
  containers:
  - name: psql-client
    image: postgres:latest
    # command: [ "sleep", "infinity" ]  # Keep the pod running for testing

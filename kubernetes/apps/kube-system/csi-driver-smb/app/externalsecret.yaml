---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: smb-secret
  namespace: kube-system
spec:
  target:
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        username: |-
          {{ .username }}
        domain: |-
          {{ .domain }}
        password: |-
          {{ .password }}
  refreshInterval: 15m
  data:
    - secretKey: domain
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: bf88639d-cd2d-450a-9fa2-46fbfd5b1cd6
        property: domain
    - secretKey: username
      sourceRef:
        storeRef:
          name: bitwarden-login
          kind: ClusterSecretStore
      remoteRef:
        key: bf88639d-cd2d-450a-9fa2-46fbfd5b1cd6
        property: username
    - secretKey: password
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: bf88639d-cd2d-450a-9fa2-46fbfd5b1cd6
        property: samba